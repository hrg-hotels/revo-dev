name: Deploy to Kinsta

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Deploy to Kinsta
      run: |
        ssh -o StrictHostKeyChecking=no hrgredesigndevelopment "
        cd /www/hrgredesigndevelopment_703/public &&
        git pull origin main &&
        npm install &&
