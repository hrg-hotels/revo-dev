name: deploy-production

on:
  push:
    branches:
      - main

concurrency:
  group: production-deploy
  cancel-in-progress: false

jobs:
  deploy:
    name: deploy-production
    runs-on: ubuntu-latest
    environment: production

    env:
      SSH_HOST: ssh.altgemeinde.project.host
      SSH_PORT: "22"
      SSH_USER: "ssh-key5dn@a-ywigtc"

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Validate secrets and target path
        shell: bash
        run: |
          set -euo pipefail
<<<<<<< HEAD
=======

>>>>>>> origin/main
          test -n "${{ secrets.SSH_PRIVATE_KEY }}"
          test -n "${{ secrets.DEPLOY_PATH }}"

          case "${{ secrets.DEPLOY_PATH }}" in
            *revo-hospitality-gro*) ;;
            *)
              echo "Refusing to deploy: DEPLOY_PATH does not point to live."
              exit 1
              ;;
          esac

      - name: Write SSH key
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p ~/.ssh
          printf '%s\n' "${{ secrets.SSH_PRIVATE_KEY }}" > ~/.ssh/deploy_key
          chmod 600 ~/.ssh/deploy_key

      - name: Add server to known_hosts
        shell: bash
        run: |
          set -euo pipefail
          ssh-keyscan -p "$SSH_PORT" "$SSH_HOST" >> ~/.ssh/known_hosts
          chmod 644 ~/.ssh/known_hosts

      - name: Verify remote live installation
        shell: bash
        run: |
          set -euo pipefail
          ssh -i ~/.ssh/deploy_key -o IdentitiesOnly=yes -o StrictHostKeyChecking=yes -p "$SSH_PORT" "$SSH_USER@$SSH_HOST" "
            set -e
            test -d '${{ secrets.DEPLOY_PATH }}'
            test -d '${{ secrets.DEPLOY_PATH }}/wp-content'
            test -d '${{ secrets.DEPLOY_PATH }}/wp-content/plugins'
            case '${{ secrets.DEPLOY_PATH }}' in
              *revo-hospitality-gro*) ;;
              *)
                echo 'Remote path is not live.'
                exit 1
                ;;
            esac
          "

<<<<<<< HEAD
      - name: Ensure whitelisted plugin directories exist
=======
      - name: Ensure plugin root exists
>>>>>>> origin/main
        shell: bash
        run: |
          set -euo pipefail
          ssh -i ~/.ssh/deploy_key -o IdentitiesOnly=yes -o StrictHostKeyChecking=yes -p "$SSH_PORT" "$SSH_USER@$SSH_HOST" "
<<<<<<< HEAD
            mkdir -p '${{ secrets.DEPLOY_PATH }}/wp-content/plugins/Hotel-Portfolio'
            mkdir -p '${{ secrets.DEPLOY_PATH }}/wp-content/plugins/Hotel-Portfolio-Marketing'
            mkdir -p '${{ secrets.DEPLOY_PATH }}/wp-content/plugins/MICE-Hotels'
            mkdir -p '${{ secrets.DEPLOY_PATH }}/wp-content/plugins/jobportal'
            mkdir -p '${{ secrets.DEPLOY_PATH }}/wp-content/plugins/revo-hotels-maps'
          "

      - name: Deploy Hotel-Portfolio
=======
            mkdir -p '${{ secrets.DEPLOY_PATH }}/wp-content/plugins'
          "

      - name: Deploy tracked plugins to live
>>>>>>> origin/main
        shell: bash
        run: |
          set -euo pipefail
          rsync -az \
            --exclude='.git/' \
            --exclude='.github/' \
            -e "ssh -i ~/.ssh/deploy_key -o IdentitiesOnly=yes -o StrictHostKeyChecking=yes -p $SSH_PORT" \
<<<<<<< HEAD
            "wp-content/plugins/Hotel-Portfolio/" \
            "$SSH_USER@$SSH_HOST:${{ secrets.DEPLOY_PATH }}/wp-content/plugins/Hotel-Portfolio/"

      - name: Deploy Hotel-Portfolio-Marketing
        shell: bash
        run: |
          set -euo pipefail
          rsync -az \
            --exclude='.git/' \
            --exclude='.github/' \
            -e "ssh -i ~/.ssh/deploy_key -o IdentitiesOnly=yes -o StrictHostKeyChecking=yes -p $SSH_PORT" \
            "wp-content/plugins/Hotel-Portfolio-Marketing/" \
            "$SSH_USER@$SSH_HOST:${{ secrets.DEPLOY_PATH }}/wp-content/plugins/Hotel-Portfolio-Marketing/"

      - name: Deploy MICE-Hotels
        shell: bash
        run: |
          set -euo pipefail
          rsync -az \
            --exclude='.git/' \
            --exclude='.github/' \
            -e "ssh -i ~/.ssh/deploy_key -o IdentitiesOnly=yes -o StrictHostKeyChecking=yes -p $SSH_PORT" \
            "wp-content/plugins/MICE-Hotels/" \
            "$SSH_USER@$SSH_HOST:${{ secrets.DEPLOY_PATH }}/wp-content/plugins/MICE-Hotels/"

      - name: Deploy jobportal
        shell: bash
        run: |
          set -euo pipefail
          rsync -az \
            --exclude='.git/' \
            --exclude='.github/' \
            -e "ssh -i ~/.ssh/deploy_key -o IdentitiesOnly=yes -o StrictHostKeyChecking=yes -p $SSH_PORT" \
            "wp-content/plugins/jobportal/" \
            "$SSH_USER@$SSH_HOST:${{ secrets.DEPLOY_PATH }}/wp-content/plugins/jobportal/"

      - name: Deploy revo-hotels-maps
        shell: bash
        run: |
          set -euo pipefail
          rsync -az \
            --exclude='.git/' \
            --exclude='.github/' \
            -e "ssh -i ~/.ssh/deploy_key -o IdentitiesOnly=yes -o StrictHostKeyChecking=yes -p $SSH_PORT" \
            "wp-content/plugins/revo-hotels-maps/" \
            "$SSH_USER@$SSH_HOST:${{ secrets.DEPLOY_PATH }}/wp-content/plugins/revo-hotels-maps/"
=======
            "wp-content/plugins/" \
            "$SSH_USER@$SSH_HOST:${{ secrets.DEPLOY_PATH }}/wp-content/plugins/"
>>>>>>> origin/main
